<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"> 
		<meta name="viewport" content="width=device-width, initial-scale=1.0"> 
		<title>Home</title>
		
		<style>
		    .menu {
            	overflow: hidden;
            }
            
            .menu a{
            	float: left;
            	display: block;
            	color: #EEE9E9;
            	text-align: center;
            	padding: 10px;
            	text-decoration: none;
            	text-transform: uppercase;
            	font-weight: bold;
            	border: 3px solid transparent;
            	margin: 0.5em 1em;
            	transition: color 0.3s, border 0.3s, opacity 0.3s;
            	transition-timing-function: ease-out;
            }
            
            .menu a:last-child:hover,
            .menu a:last-child:focus{
            	border: 3px solid transparent;
            	color: #EEE9E9;
            }
            
            .menu a:hover,
            .menu a:focus {
            	color: #c3090f;
            	border: 3px solid #c3090f;
            }

            .menu .icon {
            	display: none;
            }

		    @media only screen and (max-width: 927px), only screen and (max-device-width: 927px) {
            	.menu a:not(:first-child) {
            		display: none;
            	}
            	.menu a.icon {
            		float: right;
            		display: block;
            	}
            	
            	.menu a:first-child{
            		margin-right: 0;
            	}
            
            	.menu.responsive {
            		position: relative;
            	}
              
            	.menu.responsive a.icon {
            		position: absolute;
            		right: 0;
            		top: 0;
            	}
              
            	.menu a{
            		font-size: 1em;
            	}
              
            	.menu.responsive a {
            		float: none;
            		display: block;
            		text-align: left;
            	}
            }
            
            @media only screen and (max-width: 1024px), only screen and (max-device-width: 1024px) {
            	.container{
            		margin: 0;
            		padding: 5%;
            	}
            	
            	.menu{
            		margin:0;
            		padding-left: 0;
            	}
            	
            	.menu a:first-child{
            		margin-right: 0;
            	}
            }
		</style>
	</head>
    <body>
        <%= render 'layouts/header'%>
        <div class="menu" id="myMenu">
    		<%= link_to 'My Sheets', usuario_show_path %>
    		<%= link_to 'New Sheet', sheet_new_path %>
    		<%= link_to 'Logout', logout_path %>
    		<a href="javascript:void(0);" class="icon" onclick="myFunction()">&#9776;</a>
	    </div>
        
        <h1>Usuario: <%= @user.name %></h1>

        <div class="table">
            <%= render 'sheet/home' %>
        </div>
        <script>
    		/* Toggle between adding and removing the "responsive" class to menu when the user clicks on the icon */
    		function myFunction() {
    			var x = document.getElementById("myMenu");
    			if (x.className === "menu") {
    				x.className += " responsive";
    			} else {
    				x.className = "menu";
    			}
    		}
	    </script>
    </body>
</html>