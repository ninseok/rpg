<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"> 
		<meta name="viewport" content="width=device-width, initial-scale=1.0"> 
		<title>Register</title>
	</head>
	<body>
	  <%= render 'layouts/header' %>
	  <h1>Sign in</h1>
    <section>
      <%= form_for(:login, url: login_auth_path) do |f| %>
      <div class="input">
        <%= f.label :email %>
        <%= f.email_field :email %>
      </div>
      <div class="input">
        <%= f.label :password %>
        <%= f.password_field :password %>
      </div>
      <div class="buttonduo">
        <%= f.submit "Login", class: "btnduo" %>
      </div>
      <div class="buttonduo">
          <%= button_to "Back", root_path, :method => "get", class: "btnduo" %>
      </div>
      <% end %>
    </section>
  </body>
</html>